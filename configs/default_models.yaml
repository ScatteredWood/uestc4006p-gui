# Optional defaults for GUI startup.
# Keep model paths empty by default; users should select their own weights in the GUI.
det_model: ""
seg_model: ""

# Relative output directory name used by default.
# In source mode it resolves under project runtime path;
# in frozen mode it resolves under user runtime data path.
default_output_dir: "outputs"
